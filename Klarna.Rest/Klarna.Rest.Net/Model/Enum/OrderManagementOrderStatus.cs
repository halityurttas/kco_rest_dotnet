namespace Klarna.Rest.Net.Model.Enum
{
    public enum OrderManagementOrderStatus
    {
        AUTHORIZED,
        PART_CAPTURED,
        CAPTURED,
        CANCELLED,
        EXPIRED,
        CLOSED
    }
}